{
  "name": "hilie",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p . && node scripts/copy-test-data.cjs",
    "test": "npm run build && vitest run",
    "unit-test": "vitest run",
    "generate": "npm run build && node dist/scripts/convert.js",
    "benchmark:case1": "npm run build && node dist/scripts/benchmark-case1.js",
    "benchmark:feedback": "npm run build && node dist/scripts/benchmark-feedback-sanitization.js"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3",
    "vitest": "^4.0.17"
  }
}
