{
  "name": "hilie-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w hilie",
    "test": "npm run test -w hilie",
    "dev": "npm run dev -w demo",
    "build:demo": "npm run build -w demo"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3"
  }
}
